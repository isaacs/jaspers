JASPERS

Like PHP, but Javascript.

Using Google's v8 javascript parsing engine.


INSTALLATION

Run "make" in the project directory. This will compile all the binaries into the bin folder.

You must have scons, svn, and python installed in order to build the v8 library.